{
  "name": "eye-of-sauron",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "npm-run-all -p dev:vue dev:electron",
    "dev:vue": "vite",
    "dev:electron": "npm run transpile:electron && cross-env NODE_ENV=development electron .",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "transpile:electron": "tsc -p src/electron/tsconfig.app.json",
    "dist:mac": "npm run build && npm run transpile:electron && electron-builder --mac --arm64",
    "dist:win": "npm run build && npm run transpile:electron && electron-builder --win --x64",
    "dist:linux": "npm run build && npm run transpile:electron && electron-builder --linux --x64"
  },
  "dependencies": {
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/tsconfig": "^0.8.1",
    "cross-env": "^10.1.0",
    "electron": "^39.2.6",
    "electron-builder": "^26.0.12",
    "npm-run-all": "^4.1.5",
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vue-tsc": "^3.1.4"
  }
}
